<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.arcsoft.GraduateDesign">
    <!-- camera -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- sdcard -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- wifi internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.arcsoft.GraduateDesign.Application"
        android:allowBackup="true"
        android:icon="@mipmap/face_indentify"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <activity
            android:name="com.arcsoft.GraduateDesign.PermissionAcitivity"
            android:theme="@style/NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.arcsoft.GraduateDesign.RegisterActivity"
            android:theme="@style/NoTitleBar"></activity>
        <activity
            android:name="com.arcsoft.GraduateDesign.DetecterActivity"
            android:theme="@style/NoTitleBar"></activity>
        <activity
            android:name="com.arcsoft.GraduateDesign.MainActivity"
            android:theme="@style/NoTitleBar"></activity>
    </application>

</manifest>